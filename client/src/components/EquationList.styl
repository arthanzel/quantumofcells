.equationContainer {
    h2 {
        margin-top: 15px !important;
        text-align: center;
    }
}

.equationContainer header {
    align-items: center;
    display: flex;
    padding: 10px;

    h3 {
        font-size: 16px;
        flex: 1 1 auto;
        margin: 0;
    }
    a {
        flex: 0 0 auto;
    }
}

.equationContainer .controls {
    padding: 15px;
    text-align: center;
}